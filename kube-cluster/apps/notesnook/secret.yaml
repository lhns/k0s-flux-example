# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: notesnook
    namespace: notesnook
type: Opaque
stringData:
    NOTESNOOK_API_SECRET: CHANGEME-NOTESNOOK_API_SECRET
    SMTP_USERNAME: CHANGEME-SMTP_USERNAME
    SMTP_PASSWORD: CHANGEME-SMTP_PASSWORD
    S3_ACCESS_KEY_ID: CHANGEME-S3_ACCESS_KEY_ID
    S3_ACCESS_KEY: CHANGEME-S3_ACCESS_KEY
