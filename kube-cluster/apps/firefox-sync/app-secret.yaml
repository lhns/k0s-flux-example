# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: firefox-sync-secrets
    namespace: firefox-sync
type: Opaque
data:
    master-secret: CHANGEME-master-secret
    metrics-hash-secret: CHANGEME-metrics-hash-secret
