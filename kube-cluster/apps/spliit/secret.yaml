# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: spliit-secrets
    namespace: spliit
type: Opaque
stringData:
    S3_UPLOAD_KEY: CHANGEME-S3_UPLOAD_KEY
    S3_UPLOAD_SECRET: CHANGEME-S3_UPLOAD_SECRET
    OPENAI_API_KEY: CHANGEME-OPENAI_API_KEY
    BETTER_AUTH_SECRET: CHANGEME-BETTER_AUTH_SECRET
    EMAIL_UNSUBSCRIBE_SECRET: CHANGEME-EMAIL_UNSUBSCRIBE_SECRET
    OIDC_CLIENT_SECRET: CHANGEME-OIDC_CLIENT_SECRET
