; The image's default logs only notice/warning/error to a file, and Asterisk daemonises,
; so its console output goes nowhere. That makes "sip set debug on" invisible and leaves
; call failures with no evidence at all. Send verbose to the file too, so the SIP dialogue
; can be read after the fact.
[general]

[logfiles]
messages => notice,warning,error,verbose
