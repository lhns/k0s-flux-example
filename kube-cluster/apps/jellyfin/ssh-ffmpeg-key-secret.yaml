# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: jellyfin-ssh-ffmpeg-key
    namespace: jellyfin
    annotations:
        # Public key (not secret) — copy into the worker's root authorized_keys.
        ssh-ffmpeg.lhns.de/authorized-key: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIB4UvuGx+WpyCl5YtqI05sb8h8M9/9qUnzzP+ywH9P9+ jellyfin-rffmpeg
type: Opaque
stringData:
    id: CHANGEME-id
