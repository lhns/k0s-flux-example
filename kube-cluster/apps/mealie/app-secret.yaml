# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: mealie-secrets
    namespace: mealie
type: Opaque
stringData:
    oidc-client-secret: CHANGEME-oidc-client-secret
    openai-api-key: CHANGEME-openai-api-key
