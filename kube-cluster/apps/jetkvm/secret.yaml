# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: jetkvm
    namespace: jetkvm
type: Opaque
stringData:
    COOKIE_SECRET: CHANGEME-COOKIE_SECRET
    GOOGLE_CLIENT_ID: CHANGEME-GOOGLE_CLIENT_ID
    GOOGLE_CLIENT_SECRET: CHANGEME-GOOGLE_CLIENT_SECRET
