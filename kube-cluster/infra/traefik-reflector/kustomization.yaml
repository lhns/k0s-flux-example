apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  annotations:
    # Needs the Traefik CRDs (Middleware/TLSOption/ServersTransport) to watch.
    dependsOn: infra-traefik
resources:
- namespace.yaml
- release.yaml
