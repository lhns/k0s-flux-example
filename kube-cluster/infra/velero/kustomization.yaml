apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  annotations:
    # CSI snapshots need the snapshot.storage.k8s.io CRDs.
    dependsOn: infra-snapshot-controller
resources:
- namespace.yaml
- s3-secret.yaml
- release.yaml
