# EXAMPLE — placeholder values. In the source repo this file is SOPS-encrypted
# (age, `encrypted_regex: ^(data|stringData)$`). Replace each CHANGEME-* and
# encrypt with `sops -e -i` before applying.
apiVersion: v1
kind: Secret
metadata:
    name: opentelemetry-gateway-s3
    namespace: opentelemetry-gateway
type: Opaque
stringData:
    AWS_ACCESS_KEY_ID: CHANGEME-AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY: CHANGEME-AWS_SECRET_ACCESS_KEY
