apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# app.kubernetes.io/instance=spegel (= dir name) is applied by the root
# generator via commonMetadata — nothing to add here.
resources:
- namespace.yaml
- release.yaml
- vnet.yaml
