# Every annotation the generator reads, all non-default.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  annotations:
    dependsOn: infra-first, infra-second
    appInstance: renamed-instance
    crdPolicy: Skip
resources:
  - resources.yaml
